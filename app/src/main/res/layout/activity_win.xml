<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_win"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.m2dl.mobe.mazeltof.Activities.WinActivity">

    <TextView
        android:text="Gagné!"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_marginTop="228dp"
        android:id="@+id/wintv"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="20pt"
        android:textAlignment="center"/>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/retour"
        android:layout_alignTop="@+id/boutonRejouer"
        android:layout_toEndOf="@+id/wintv"
        android:id="@+id/boutonMenu" />

    <TextView
        android:text="00:00:000"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_below="@+id/wintv"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="75dp"
        android:id="@+id/tempsMenu"
        android:textSize="14pt"
        android:textAlignment="center" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/rejouer"
        android:layout_marginTop="110dp"
        android:id="@+id/boutonRejouer"
        android:layout_below="@+id/tempsMenu"
        android:layout_toStartOf="@+id/wintv" />

</RelativeLayout>
